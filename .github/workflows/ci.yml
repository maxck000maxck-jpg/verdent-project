# Conditional Workflow to Build USBPDRoleSwap

name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Check for Directory
        id: check_directory
        run: |
          if [ -d "USBPDRoleSwap" ]; then
            echo "directory exists";
          else
            echo "directory does not exist";
            exit 1;
          fi

      - name: Build USBPDRoleSwap
        if: steps.check_directory.outputs.success == 'true'
        run: |
          echo "Building USBPDRoleSwap..."
          # Add your build commands here
